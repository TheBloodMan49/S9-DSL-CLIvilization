# Game runs without .env but AI players won't work without OPENAI_KEY set.

# AI Type (optional, defaults to llm)
# Options: 
#   - random: Simple random AI that makes random decisions (no API key needed)
#   - llm: Language model-based AI that uses strategic reasoning (requires OPENAI_KEY)
AI_TYPE=llm

# OpenAI API Key (required for LLM AI players, not needed for random AI)
# Get from: https://platform.openai.com/api-keys or https://openrouter.ai/keys
OPENAI_KEY=

# API Endpoint (optional, defaults to OpenAI)
# Examples:
#   - OpenRouter: https://openrouter.ai/api/v1
#   - Local LLM: http://localhost:1234/v1
OPENAI_BASE_URL=https://openrouter.ai/api/v1

# AI Model (optional, defaults to openai/gpt-4o-mini)
# Options: openai/gpt-4o-mini, openai/gpt-4o, anthropic/claude-3-haiku, etc.
AI_MODEL=openai/gpt-4o-mini

# Logging Level (optional, defaults to info)
# Options: off, error, warn, info, debug, trace
# Logs are written to output/game.log (cleared on startup)
LOG_LEVEL=
